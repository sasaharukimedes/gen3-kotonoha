<!DOCTYPE html>
<html lang="ja" >
  <head>
    <title><%= full_title(yield(:title)) %></title>
    <%= render 'layouts/rails_default' %>
  </head>

  <body class="font-kiwi bg-back text-body">
    <%= render 'shared/header' %>
    <main class="container mx-auto">
      <%= yield %>
    </main>
    <%= render 'shared/footer' %>
  </body>
</html>